spring:
  application:
    name: cloudpay
  zipkin:
    base-url: http://localhost:9411

server:
  port: 7080

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8001/eureka
